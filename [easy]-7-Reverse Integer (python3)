class Solution(object):
    def reverse(self, x):
        """
        :type x: int
        :rtype: int
        """
       
        r= int(str(x)[::-1]) if x>=0 else - int(str(x*-1)[::-1])
        return r if  ( r>=(-(2 ** 31)) and  r<=((2 ** 31)-1)) else  0
       
